file_scanner.settings:
  type: config_object
  label: 'FileScanner settings'
  mapping:
    scan_directory:
      type: string
      label: 'Directory to scan'
    extensions:
      type: sequence
      label: 'Allowed file extensions'
      sequence:
        type: string
    media_bundle:
      type: string
      label: 'Target media bundle'
